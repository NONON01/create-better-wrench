# JVM/Gradle 参数
org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Parchment 映射(1.21.1 的官方推荐)
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17

# Minecraft / NeoForge
minecraft_version=1.21.1
minecraft_version_range=[1.21.1]
# NeoForge 版本见 https://projects.neoforged.net/neoforged/neoforge
neo_version=21.1.249
loader_version_range=[1,)

# 本 Mod 属性
mod_id=create_better_wrench
# 2026-09-18 改名: 原名 Universal Wrench 与 Modrinth 上已有的同名项目(同为扳手工具类)
# 冲突, 故改为 Create Better Wrench —— 与 mod_id 一致, 也符合主流附属命名
# (Create Aeronautics / Create Big Cannons, 均为 "Create X" 无冒号形式)。
# ⚠️ 物品本身仍叫 Universal Wrench(见 lang 文件), 只有"模组名"改了。
mod_name=Create Better Wrench
# 许可证字段做成了"指针式"(和 Create 的做法一致: 它写的是 "Read attached LICENSE.md")。
# 指向 jar 内的 LICENSE.md —— 那**一个**文件里同时包含:本模组条款、全部第三方声明、
# 以及 Create 的 MIT 许可全文(逐字收录, 满足 MIT 的硬性要求)。
mod_license=Read attached LICENSE.md
# 2026-09-18: 0.4.0+create6.0.10 -> 1.0.0-beta。
# ⚠️ 用带连字符的 `1.0.0-beta`(标准 SemVer 预发布写法), 不是 `1.0.0beta` ——
#    Modrinth 明确要求 beta/alpha 走 SemVer 形式, 否则上传时会触发版本号验证错误:
#    https://support.modrinth.com/en/articles/8793369-version-number-recommendations
# (原先的 `+create6.0.10` 构建元数据后缀本次去掉; 若想保留, 写成 `1.0.0-beta+create6.0.10` 即可。)
mod_version=1.0.0-beta
mod_group_id=com.nonono.createbetterwrench

# ---- 机械动力 Create 集成 ----
# 本 mod 是纯 Create addon(核心 = 更好用的 Create 扳手), 故默认 enableCreate=true
enableCreate=true
# Create 6.x (NeoForge 1.21.1)，取自 https://wiki.createmod.net/developers/depend-on-create
# 6.0.10-280 = 已发布版 6.0.10 在 maven 上对应的 dev/CI 构建号
create_version=6.0.10-280
ponder_version=1.0.82
flywheel_version=1.0.6
registrate_version=MC1.21-1.3.0+67
